# Runtime dependencies — DAEP / RE-OS Python service scaffold.
# Authority: WP-001-08 | DEPENDENCY_POLICY.md §2
#
# This is the human-authored source file. Floating ranges are permitted here.
# The compiled, exact-pinned output is requirements.txt — do not edit it by hand.
#
# Regenerate requirements.txt after any change to this file:
#   pip-compile requirements.in -o requirements.txt
#
# Scan for CVEs after regenerating:
#   pip-audit -r requirements.txt --desc on
#
# Version constraints below mirror pyproject.toml [project].dependencies.
# Both files must be kept in sync — update pyproject.toml and requirements.in together.

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic-settings>=2.3.0
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0
structlog>=24.1.0
python-jose[cryptography]>=3.3.0

# Shared platform libraries (EPIC-002) — internal index, ARTIFACT_REPOSITORY.md
# pip-compile requires: --extra-index-url http://localhost:8080/simple/
reos-config>=0.1.0
reos-logging>=0.1.0
reos-exceptions>=0.1.0
reos-common>=0.1.0
