# requirements.txt — DAEP / RE-OS Python service scaffold
# Authority: WP-001-08 | DEPENDENCY_POLICY.md §2
#
# DO NOT EDIT — this file is generated by pip-compile from requirements.in.
# Source:     requirements.in
# Regenerate: pip-compile requirements.in -o requirements.txt --extra-index-url http://localhost:8080/simple/
# CVE scan:   pip-audit -r requirements.txt --desc on
# Python:     3.11
#
# ============================================================================
# IMPORTANT: Regenerate before deploying any service built from this scaffold.
# The pins below are representative and must be resolved in your environment:
#
#   pip install pip-tools pip-audit
#   pip-compile requirements.in -o requirements.txt --extra-index-url http://localhost:8080/simple/
#   pip-audit -r requirements.txt --desc on
#
# Zero known CVEs are required before committing the regenerated file.
#
# NOTE (WP-003-01): reos-config/reos-logging/reos-exceptions/reos-common were
# added to pyproject.toml by EPIC-002 (WP-002-01/03/05/07) but requirements.in
# was not regenerated at that time — DEPENDENCY_POLICY.md §4 requires both
# files change together; this drift is closed below. Entries are added by
# hand (no pip-compile/network access to the internal index in this build
# environment) and MUST be replaced by a real pip-compile run — per the
# regenerate note above — before this scaffold is deployed anywhere.
# ============================================================================

# FastAPI web framework
fastapi==0.111.1
    # via requirements.in

# Starlette (ASGI toolkit — fastapi dependency)
starlette==0.37.2
    # via fastapi

# Uvicorn ASGI server with standard extras
uvicorn==0.30.1
    # via requirements.in
click==8.1.7
    # via uvicorn
h11==0.14.0
    # via uvicorn
httptools==0.6.1
    # via uvicorn[standard]
python-dotenv==1.0.1
    # via uvicorn[standard]
uvloop==0.19.0
    # via uvicorn[standard]
watchfiles==0.22.0
    # via uvicorn[standard]
websockets==12.0
    # via uvicorn[standard]

# Pydantic v2 data validation
pydantic==2.8.2
    # via fastapi, pydantic-settings
pydantic-core==2.20.1
    # via pydantic
annotated-types==0.7.0
    # via pydantic

# Pydantic Settings (.env file support)
pydantic-settings==2.3.4
    # via requirements.in

# SQLAlchemy async ORM
sqlalchemy==2.0.31
    # via requirements.in
greenlet==3.0.3
    # via sqlalchemy (async mode)

# Alembic database migrations
alembic==1.13.2
    # via requirements.in
mako==1.3.5
    # via alembic
markupsafe==2.1.5
    # via mako

# AsyncPG — async PostgreSQL driver
asyncpg==0.29.0
    # via requirements.in

# Structlog — structured logging
structlog==24.2.0
    # via requirements.in

# python-jose JWT handling with cryptography backend
python-jose==3.3.0
    # via requirements.in
cryptography==42.0.8
    # via python-jose[cryptography]
cffi==1.16.0
    # via cryptography
pycparser==2.22
    # via cffi

# Shared platform libraries (EPIC-002) — internal index, ARTIFACT_REPOSITORY.md
reos-config==0.1.0
    # via requirements.in
reos-logging==0.1.0
    # via requirements.in, reos-exceptions
reos-exceptions==0.1.0
    # via requirements.in
reos-common==0.1.0
    # via requirements.in

# Shared transitive dependencies
anyio==4.4.0
    # via fastapi, starlette
exceptiongroup==1.2.2
    # via anyio
idna==3.7
    # via anyio
sniffio==1.3.1
    # via anyio
typing_extensions==4.12.2
    # via pydantic, sqlalchemy, alembic, anyio
