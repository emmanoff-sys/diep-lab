---
# DAEP / RE-OS — shared-dev Ansible inventory (WP-003-14)
#
# PROVISIONING STATUS: STRUCTURALLY READY — populate with real host IPs once
# the first VM is provisioned via `terraform/environments/shared-dev/terraform.tfvars`.
# See ENVIRONMENT_STRATEGY.md (WP-003-14) for live-vs-deferred status.
# Auto-deploy trigger: every push to `develop` (Roadmap v1.0 §11.2).

all:
  vars:
    reos_environment: "shared_dev"
    admin_network_cidr: "10.0.0.0/8"
    consul_server_addr: "consul.shared-dev.reos.internal"
    vault_addr: "https://vault.shared-dev.reos.internal:8200"
    consul_version: "1.18.1"
    node_exporter_version: "1.8.1"

  children:
    web:
      hosts:
        # Populated with real host IP once Terraform provisioning runs:
        # reos-shared-dev-web-01.internal:
        #   ansible_host: 10.0.1.10
    application:
      hosts:
        # reos-shared-dev-app-01.internal:
        #   ansible_host: 10.0.1.11
