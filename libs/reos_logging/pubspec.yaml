# reos_logging — DAEP / RE-OS shared client-side logging (Flutter).
# Authority: WP-002-04 | DRDP v1.0 §22 (State Management), §23.1 (Flutter architecture).
# Consumed via monorepo path import alongside reos_config.

name: reos_logging
description: DAEP / RE-OS structured client-side logging for Flutter apps.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  reos_config:
    path: ../reos_config

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
